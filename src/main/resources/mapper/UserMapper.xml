<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stms.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.stms.model.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, real_name, role, created_at
        FROM t_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, username, password, real_name, role, created_at
        FROM t_user
        WHERE id = #{id}
    </select>

    <update id="updatePassword" parameterType="com.stms.model.User">
        UPDATE t_user SET password = #{password} WHERE id = #{id}
    </update>

</mapper>
